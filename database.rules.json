{
  "rules": {
    "users": {
      "$uid": {
        "budget": {
          "transactions": {
            ".read": "$uid === auth.uid",
            ".write": "$uid === auth.uid"
          }
        }
      }
    }
  }
}
